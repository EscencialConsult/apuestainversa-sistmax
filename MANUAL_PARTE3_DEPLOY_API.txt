================================================================
INVERSA.Bid â€” SISTEMA INTEGRAL DE COMPRAS Y SUBASTAS TRANSPARENTES
MANUAL TÃ‰CNICO â€” PARTE 3: DEPLOY, API Y CONFIGURACIÃ“N
VersiÃ³n: v1.0.0-rc2 | Junio 2026
================================================================

ÃNDICE
------
1.  Setup del entorno de desarrollo
2.  Variables de entorno
3.  Build de producciÃ³n
4.  Deploy on-premise (IIS)
5.  Deploy on-premise (Apache / Nginx)
6.  Onboarding de un nuevo tenant (municipio)
7.  Contratos de API â€” todos los endpoints requeridos
8.  Demo flags (URL params para testing)
9.  Pendientes tÃ©cnicos
10. Referencia de versiones y branches


================================================================
1. SETUP DEL ENTORNO DE DESARROLLO
================================================================

PREREQUISITOS:
  Node.js 20+
  npm 10+

INSTALACIÃ“N:
  git clone <repo>
  cd max_smt
  npm install

ARRANCAR EL DEV SERVER:
  npm run dev
  â†’ Disponible en http://localhost:5173

SELECCIÃ“N DE TENANT EN DESARROLLO:
  La app carga el tenant "sanmiguel" por defecto (DEFAULT_TENANT_ID).
  Para probar otro tenant:
    http://localhost:5173?tenant=demo
  Para persistir la selecciÃ³n en el navegador:
    localStorage.setItem('sicst_tenant_dev', 'demo')

FALLBACK SIN BACKEND:
  Si el backend no estÃ¡ corriendo, el TenantContext detecta
  import.meta.env.DEV y cae automÃ¡ticamente al config local
  del archivo src/config/tenants.js.
  No aparece ningÃºn error â€” la app carga con el branding hardcodeado.

AUDITORÃA DE CALIDAD:
  npm run doctor                    Scan completo del codebase
  npx react-doctor@latest --diff    Solo archivos cambiados vs master
  Score mÃ­nimo aceptable: 100/100


================================================================
2. VARIABLES DE ENTORNO
================================================================

ARCHIVO DE EJEMPLO: .env.example

  VITE_API_URL=/api
  (URL base de la API. Con / al inicio = relativo al mismo dominio)

ARCHIVOS POR ENTORNO:
  .env.local         Desarrollo local (ignorado por git â€” no commitear)
  .env.production    ProducciÃ³n (commiteado, sin secrets)

PARA PRODUCCIÃ“N ON-PREMISE (mismo servidor):
  VITE_API_URL=/api
  El IIS hace reverse proxy de /api/* â†’ backend

PARA PRODUCCIÃ“N CON API EN SUBDOMINIO SEPARADO:
  VITE_API_URL=https://api.sicst.app
  (Agregar CORS en el backend para el dominio del frontend)

NOTA: Vite solo expone variables que empiezan con VITE_.
En el cÃ³digo se acceden como import.meta.env.VITE_API_URL.
Las variables se resuelven en BUILD TIME, no en runtime.


================================================================
3. BUILD DE PRODUCCIÃ“N
================================================================

COMANDO:
  npm run build

OUTPUT: carpeta dist/
  dist/index.html       Punto de entrada (0.78 kB)
  dist/assets/          Todos los chunks JS, CSS y fuentes

CHUNKS PRINCIPALES (gzip):
  vendor-react.js    59 kB   React + React DOM + React Router
  index.js           22 kB   Runtime compartido + contexts + layouts
  index.css          22 kB   Tailwind CSS compilado
  vendor-util.js      9 kB   axios + clsx + tailwind-merge
  DetalleCompra.js    9 kB   Vista maestra (mÃ³dulo mÃ¡s grande)
  vendor-ui.js        6 kB   lucide-react (tree-shaken)
  PortalPage.js       6 kB   Portal Ciudadano completo
  (resto de pÃ¡ginas: 1-6 kB cada una)

El contenido de dist/ es 100% estÃ¡tico. No requiere Node.js en producciÃ³n.
El servidor solo necesita servir archivos estÃ¡ticos y redirigir al index.html.

VERIFICAR BUILD LOCALMENTE:
  npm run preview
  â†’ Disponible en http://localhost:4173


================================================================
4. DEPLOY ON-PREMISE â€” IIS (WINDOWS SERVER)
================================================================

PASOS:

  1. Generar el build:
       npm run build

  2. Copiar dist/ al webroot del IIS:
       C:\inetpub\wwwroot\sicst-max\   (o la ruta que configure infra)

  3. Verificar que el mÃ³dulo URL Rewrite estÃ© instalado en IIS.
     Sin Ã©l, las rutas tipo /compras/EXP-0001 devuelven 404.
     (Se instala desde el Web Platform Installer de Microsoft)

  4. El archivo dist/web.config ya estÃ¡ incluido y configurado.
     No requiere modificaciÃ³n. Contiene:
       - Regla SPA Fallback: toda ruta no-archivo â†’ index.html
       - MIME type para .woff2 (fuentes)

  5. Configurar el Application Pool en modo "No Managed Code"
     (el sitio no usa .NET, es 100% estÃ¡tico)

  6. Si hay reverse proxy hacia el backend:
     Agregar una segunda regla en web.config para /api/* â†’ backend:

     <rule name="API Proxy" stopProcessing="true">
       <match url="^api/(.*)" />
       <action type="Rewrite" url="http://localhost:3000/api/{R:1}" />
     </rule>

  7. En el Application Pool, habilitar SSL si el subdominio usa HTTPS.

VERIFICACIÃ“N:
  - Abrir sanmiguel.sicst.app â†’ debe cargar la app
  - Navegar a /compras y recargar (F5) â†’ debe seguir en /compras (no 404)
  - La barra de URL debe mantener el subdominio correcto


================================================================
5. DEPLOY ON-PREMISE â€” APACHE / NGINX
================================================================

APACHE:
  El archivo dist/.htaccess ya estÃ¡ incluido. Contiene:
    RewriteEngine On
    RewriteBase /
    RewriteRule ^index\.html$ - [L]
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule . /index.html [L]

  Requisitos:
    - mod_rewrite habilitado
    - AllowOverride All en el VirtualHost del sitio

NGINX:
  El .htaccess no aplica en Nginx. Agregar en la config del servidor:

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }


================================================================
6. ONBOARDING DE UN NUEVO TENANT (MUNICIPIO)
================================================================

Para dar de alta un nuevo municipio en la plataforma sin tocar cÃ³digo:

  PASO 1: CONFIGURAR DNS
    Crear registro A o CNAME: nuevomuni.sicst.app â†’ IP del servidor
    (Si hay wildcard *.sicst.app ya configurado, esto es automÃ¡tico)

  PASO 2: INSERTAR EN BASE DE DATOS
    INSERT en la tabla TenantProfile:
      tenant_id     "nuevomuni"
      name          "Municipio de Nueva Ciudad"
      short_name    "NVC"
      logo_url      "/tenants/nuevomuni/logo.svg"  (o null)
      favicon_url   null
      theme         { JSON con todas las variables CSS â€” ver estructura abajo }

    Estructura del campo theme (JSON):
      {
        "--color-primary":   "R G B",   (ej: "0 212 255" para cyan)
        "--color-secondary": "R G B",
        "--color-accent":    "R G B",
        "--color-success":   "R G B",
        "--color-warning":   "R G B",
        "--color-danger":    "R G B",
        "--color-bg":        "R G B",   (fondo oscuro, ej: "3 7 18")
        "--color-surface":   "R G B",   (superficies, ej: "10 18 40")
        "--color-border":    "R G B",   (bordes, ej: "30 50 90")
        "--color-text":      "R G B",   (texto principal)
        "--color-muted":     "R G B"    (texto secundario)
      }
    IMPORTANTE: Los valores son "R G B" en decimal (sin comas, sin #).
    Tailwind los consume con esta sintaxis para soportar opacidad:
      bg-tenant-primary/50  â†’  background: rgb(R G B / 0.5)

  PASO 3: SUBIR ASSETS
    Subir el logo al servidor de assets en la ruta configurada.
    Formato recomendado: SVG (vectorial, sin pÃ©rdida de calidad).

  PASO 4: CREAR USUARIOS INICIALES
    Insertar en la tabla de usuarios del tenant:
      - Al menos 1 admin
      - Los usuarios que el municipio necesite por rol

  PASO 5: VERIFICAR
    En navegador: https://nuevomuni.sicst.app
    Debe cargar con los colores del nuevo municipio.
    En desarrollo: http://localhost:5173?tenant=nuevomuni

  PASO 6: CONFIGURAR PARÃMETROS MUNICIPALES
    Desde la cuenta admin del nuevo tenant, configurar:
      - Modalidades de contrataciÃ³n permitidas
      - JerarquÃ­a de aprobaciÃ³n (quiÃ©n firma, en quÃ© orden)
      - Plantilla de ResoluciÃ³n de AdjudicaciÃ³n
      - Plantilla de Orden de Compra
      - Plantilla de Acta de RecepciÃ³n Conforme
    Sin estos parÃ¡metros, los mÃ³dulos correspondientes mostrarÃ¡n
    EmptyState isMandatory y no podrÃ¡n usarse.


================================================================
7. CONTRATOS DE API â€” TODOS LOS ENDPOINTS REQUERIDOS
================================================================

A continuaciÃ³n se listan TODOS los endpoints que el backend debe
implementar para que el frontend funcione en producciÃ³n.
Los marcados con [MOCK] tienen implementaciÃ³n mock en los servicios;
los marcados con [PENDIENTE] aÃºn no tienen mock.

--- TENANT ---

  GET /api/tenants/:tenantId/branding
  [OPERATIVO EN BACKEND]
  Response:
    {
      "id": "sanmiguel",
      "name": "Municipio de San Miguel de TucumÃ¡n",
      "shortName": "SMT",
      "logoUrl": "/tenants/sanmiguel/logo.svg",
      "faviconUrl": null,
      "theme": { "--color-primary": "0 212 255", ... }
    }
  404 si tenant_id no existe â†’ frontend muestra pantalla fatal

--- AUTENTICACIÃ“N ---

  POST /api/auth/login
  [PENDIENTE â€” actualmente mock local]
  Body:   { "email": "...", "password": "..." }
  Response: { "token": "jwt...", "user": { id, name, email, role, tenantId } }
  401 si credenciales incorrectas

--- PORTAL CIUDADANO (pÃºblico, sin auth) ---

  GET /api/portal/:tenantId/procesos
  [MOCK en portalService.js]
  Response: [ { id, expedienteNum, titulo, area, modalidad, estado,
                montoBase, montoAdjudicado, ganador, fechaSubasta,
                fechaAdjudicacion } ]
  Solo retorna estado PUBLICADO y ADJUDICADO. Nunca borradores.

  GET /api/portal/:tenantId/documentos
  [MOCK en portalService.js]
  Response: [ { id, expedienteNum, tipo, titulo, fechaEmision, contenido } ]
  El campo contenido es texto plano ya renderizado (listo para descargar).

--- PROVEEDORES ---

  GET /api/:tenantId/proveedores
  [MOCK] Lista de proveedores del tenant

  POST /api/:tenantId/proveedores
  [MOCK] Crear nuevo proveedor

  GET /api/arca/verificar/:cuit
  [MOCK con contingencia] VerificaciÃ³n ARCA/AFIP
  Si falla (503) el frontend activa el formulario de carga manual.

--- COMPRAS / EXPEDIENTES ---

  GET /api/:tenantId/compras
  [MOCK] Lista de expedientes del tenant

  POST /api/:tenantId/compras
  [MOCK] Crear nuevo expediente

  GET /api/:tenantId/compras/:id
  [MOCK en expedienteService.js] Detalle completo del expediente
  Response incluye: datos generales, proveedores, lances, circuito
  de aprobaciÃ³n, documentos generados, recepciÃ³n (si existe)

  POST /api/:tenantId/compras/:id/recepcion
  [MOCK] Registrar recepciÃ³n de bienes
  Body: { fechaRecepcion, lugarRecepcion, responsableRecepcion, observaciones }
  Response: { ok, numActa, timestamp }
  AdemÃ¡s debe escribir en la bitÃ¡cora de auditorÃ­a.

--- SUBASTA ---

  WebSocket: ws://servidor/api/:tenantId/subastas/:id/live
  [MOCK con MockWebSocket] Lances en tiempo real
  El servidor emite eventos de tipo: LANCE, INICIO, CIERRE, ANTINIPER
  El cliente envÃ­a: { tipo: "LANCE", monto: 500000 }

--- ADJUDICACIÃ“N ---

  GET /api/:tenantId/adjudicacion/plantillas
  [MOCK en adjudicacionService.js] Plantillas de contrato y orden

  POST /api/:tenantId/adjudicacion/:id/firmar
  [MOCK] Firmar etapa del circuito
  Body: { etapaId, accion: "aprobar"|"rechazar", firmante, observaciones }

--- APROBACIÃ“N ---

  GET /api/:tenantId/aprobacion/jerarquias
  [MOCK en aprobacionService.js] Config de jerarquÃ­as de aprobaciÃ³n
  Retorna null si no hay jerarquÃ­as configuradas â†’ EmptyState isMandatory

  GET /api/:tenantId/aprobacion/expedientes
  [MOCK] Lista de expedientes pendientes para el usuario logueado

  GET /api/:tenantId/aprobacion/expedientes/:id
  [MOCK] Detalle completo del expediente para aprobar

  POST /api/:tenantId/aprobacion/:id/firmar
  [MOCK] Firmar etapa del circuito de aprobaciÃ³n

--- AUDITORÃA ---

  GET /api/:tenantId/auditoria
  [MOCK en auditoriaService.js]
  Query params: fechaDesde, fechaHasta, modulo, tipo, q (bÃºsqueda)
  Response: { eventos: [...], total: N }

  POST /api/:tenantId/auditoria/evento
  [MOCK â€” logEventoExterno()] Escritura de evento externo
  Llamado silenciosamente desde otros mÃ³dulos (recepciÃ³n, adjudicaciÃ³n)

--- CONFIGURACIÃ“N (admin) ---

  GET /api/:tenantId/config/plantillas
  [MOCK] Retorna plantillas configuradas (conformidad, contrato, orden)
  Retorna null por tipo si no estÃ¡ configurada â†’ EmptyState isMandatory

  GET /api/:tenantId/config/modalidades
  [MOCK] Retorna modalidades de contrataciÃ³n habilitadas
  Retorna null si no hay â†’ EmptyState isMandatory en Paso 3 del wizard


================================================================
8. DEMO FLAGS â€” URL PARAMS PARA TESTING
================================================================

Estos parÃ¡metros de URL activan estados especiales para demostrar
los EmptyState bloqueantes y condiciones de error sin modificar datos.

  ?sinJerarquias
    MÃ³dulo: AprobaciÃ³n (/aprobacion)
    Efecto: Simula que el municipio no configurÃ³ la jerarquÃ­a de firmas.
            La bandeja de aprobaciÃ³n muestra EmptyState isMandatory.

  ?sinPlantillaContrato
    MÃ³dulo: AdjudicaciÃ³n (/adjudicacion) â†’ tab ResoluciÃ³n
    Efecto: Simula plantilla de ResoluciÃ³n de AdjudicaciÃ³n no configurada.
            VisorPlantillas muestra EmptyState isMandatory en ese tab.

  ?sinPlantillaOrden
    MÃ³dulo: AdjudicaciÃ³n (/adjudicacion) â†’ tab Orden de Compra
    Efecto: Simula plantilla de Orden de Compra no configurada.
            VisorPlantillas muestra EmptyState isMandatory en ese tab.

  ?sinDocConformidad
    MÃ³dulo: RecepciÃ³n (/compras/:id â†’ Tab RecepciÃ³n)
    Efecto: Simula plantilla de Acta de Conformidad no configurada.
            El formulario de recepciÃ³n es reemplazado por EmptyState isMandatory.

  ?sinModalidades
    MÃ³dulo: Wizard de Compra (/compras/nueva â†’ Paso 3)
    Efecto: Simula que no hay modalidades de contrataciÃ³n configuradas.
            El Paso 3 muestra EmptyState isMandatory y bloquea el avance.

  ?arcaCaida
    MÃ³dulo: Wizard de Proveedores (/proveedores/nuevo â†’ Paso 2)
    Efecto: Simula que la API de ARCA/AFIP no responde.
            El sistema muestra el formulario de carga manual de contingencia.


================================================================
9. PENDIENTES TÃ‰CNICOS
================================================================

PRIORIDAD ALTA (bloquean funcionalidad en producciÃ³n):

  1. Reemplazar mock auth por POST /api/auth/login real
     Archivo: src/services/api/authService.js (o similar)
     Token JWT debe incluir tenantId para validaciÃ³n server-side

  2. WebSocket real para el Trading Room
     Actualmente: MockWebSocket en src/pages/compras/TradingRoomPage.jsx
     Reemplazar por WebSocket nativo o socket.io apuntando al backend

  3. Todos los endpoints marcados [MOCK] en la secciÃ³n 7
     Los servicios en src/services/api/ ya tienen la estructura esperada;
     solo hay que reemplazar los datos hardcodeados por fetch reales

PRIORIDAD MEDIA:

  4. Panel de administraciÃ³n ONE (/admin)
     Para que ONE pueda crear y gestionar tenants sin ir directo a la DB.
     Incluye: crear tenant, subir logo, elegir paleta, activar mÃ³dulos.

  5. /proveedores/arca â€” placeholder pendiente
     MÃ³dulo de detalle de verificaciÃ³n ARCA para el proveedor.

  6. /documentos â€” placeholder pendiente
     Repositorio documental del tenant.

PRIORIDAD BAJA:

  7. Tests automatizados (E2E y unitarios)
     El codebase no tiene test suite aÃºn.
     react-doctor garantiza calidad de cÃ³digo pero no cubre lÃ³gica de negocio.

  8. InternacionalizaciÃ³n (i18n)
     Sistema actual solo en espaÃ±ol. Para municipios de otras provincias
     puede ser necesario soporte multi-idioma.


================================================================
10. REFERENCIA DE VERSIONES Y BRANCHES
================================================================

TAGS DE VERSIÃ“N:
  v1.0.0-rc1   Primera Release Candidate â€” Core completo + Portal Ciudadano
  v1.0.0-rc2   Segunda Release Candidate â€” TenantContext dinÃ¡mico (SaaS real)

BRANCHES:
  master                    Rama principal, cÃ³digo aprobado para producciÃ³n
  feature/portal-ciudadano  (mergeado) Portal Ciudadano
  feature/dynamic-branding  (mergeado) TenantContext dinÃ¡mico
  feature/aprobacion        (mergeado) MÃ³dulo de AprobaciÃ³n
  feature/expediente-recepcion (mergeado) Vista Maestra + RecepciÃ³n
  feature/auditoria         (mergeado) MÃ³dulo de AuditorÃ­a
  feature/trading-room      (mergeado) Trading Room

FLUJO DE TRABAJO GIT:
  1. Crear branch desde master: git checkout -b feature/nombre
  2. Desarrollar con commits atÃ³micos y descriptivos
  3. Correr react-doctor --diff antes de cada commit
  4. Merge a master con --no-ff (preserva historial del feature)
  5. Tag de versiÃ³n si corresponde release

REPOSITORIO REMOTO:
  https://github.com/EscencialConsult/apuestainversa-sistmax.git
  Branch principal: main


================================================================
FIN DEL MANUAL TÃ‰CNICO INVERSA.Bid v1.0.0-rc2
================================================================

Para consultas tÃ©cnicas sobre este sistema comunicarse con ONE.
Las tres partes de este manual cubren la totalidad del sistema:
  PARTE 1: VisiÃ³n, Arquitectura, Roles y Patrones
  PARTE 2: DescripciÃ³n funcional de cada mÃ³dulo
  PARTE 3: Deploy, API, Demo flags y Pendientes
================================================================

