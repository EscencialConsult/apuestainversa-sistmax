# SICST MAX — variables de entorno (Vite expone solo las que empiezan con VITE_)
# Copiar a .env.local y ajustar según el entorno.

# URL base de la API backend (on-premise). Default: /api (mismo origen, vía reverse proxy)
VITE_API_URL=/api
