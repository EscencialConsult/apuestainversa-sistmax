================================================================
INVERSA.Bid â€” SISTEMA INTEGRAL DE COMPRAS Y SUBASTAS TRANSPARENTES
MANUAL TÃ‰CNICO â€” PARTE 1: VISIÃ“N, ARQUITECTURA Y ROLES
VersiÃ³n: v1.0.0-rc2 | Junio 2026
================================================================

ÃNDICE
------
1.  QuÃ© es INVERSA.Bid
2.  Modelo de negocio (ONE como operador SaaS)
3.  Stack tecnolÃ³gico
4.  Arquitectura del frontend
5.  Sistema multi-tenant
6.  AutenticaciÃ³n y roles de usuario
7.  Routing y acceso por rol
8.  Patrones de ingenierÃ­a obligatorios
9.  Estructura de archivos clave


================================================================
1. QUÃ‰ ES INVERSA.Bid
================================================================

INVERSA.Bid es una plataforma SaaS de compras pÃºblicas digitales para
municipios. Digitaliza el proceso completo de adquisiciÃ³n:

  Registro de proveedores
    â†’ CreaciÃ³n del expediente de compra
      â†’ Subasta inversa en tiempo real
        â†’ AdjudicaciÃ³n con circuito de firmas
          â†’ RecepciÃ³n de bienes con acta oficial
            â†’ AuditorÃ­a inmutable

AdemÃ¡s expone un Portal Ciudadano pÃºblico para que cualquier
ciudadano consulte procesos adjudicados y descargue documentos
oficiales sin necesidad de iniciar sesiÃ³n.

Operado por ONE, quien lo habilita por municipio (tenant).
Cada municipio opera en su propio entorno aislado, con su identidad
visual y sus datos, sobre el mismo core de cÃ³digo.


================================================================
2. MODELO DE NEGOCIO â€” ONE COMO OPERADOR SAAS
================================================================

ONE es la empresa propietaria y operadora de INVERSA.Bid.

Los clientes de ONE son municipios o empresas pÃºblicas. Cada cliente
es un "tenant" con:
  - Subdominio propio:  sanmiguel.sicst.app
  - Branding propio:    colores, logo, nombre
  - Base de datos propia: datos completamente aislados de otros tenants

Para activar un nuevo cliente:
  1. Configurar DNS: agregar subdominio wildcard (*.sicst.app â†’ servidor)
  2. Insertar registro en la tabla TenantProfile de la DB de ONE
  3. Subir logo al servidor de assets
  4. Verificar con ?tenant=<id> en localhost
  5. Confirmar acceso desde el subdominio real

NO se requiere tocar cÃ³digo fuente ni redesplegar el frontend.
Esto es posible porque el TenantContext del frontend carga el
branding dinÃ¡micamente desde la API en cada arranque.


================================================================
3. STACK TECNOLÃ“GICO
================================================================

FRONTEND (este repositorio):
  - Vite 8            Bundler (usa rolldown internamente)
  - React 19          UI library (hooks, Suspense, lazy)
  - React Router v7   Routing (BrowserRouter, lazy pages, Outlet)
  - Tailwind CSS v3   Estilos (variables CSS custom para theming)
  - Lucide React      IconografÃ­a (tree-shaken automÃ¡ticamente)
  - Axios             HTTP client
  - clsx + tailwind-merge  Utilitarios de clases CSS

TIPOGRAFÍA (ONE brand — cargadas desde Google Fonts en index.html):
  - Exo 2             Títulos y headings grandes  (--font-display)
  - Josefin Sans      Subtítulos                  (--font-subtitle)
  - Inter             Cuerpo de texto             (--font-sans)
  - JetBrains Mono    Código / monospace          (--font-mono)

HERRAMIENTAS DE CALIDAD:
  - react-doctor      AuditorÃ­a automÃ¡tica (mantenida en 100/100)
  - ESLint            Linting
  - Vite preview      Preview del build de producciÃ³n

BUILD DE PRODUCCIÃ“N:
  - npm run build     Genera dist/ con chunks separados por pÃ¡gina
  - Vendor chunks:    vendor-react (189 kB), vendor-ui (18 kB), vendor-util (27 kB)
  - Page chunks:      cada pÃ¡gina es un chunk lazy de 1-33 kB
  - CSS:              index.css (73 kB) â€” Tailwind completo

BACKEND (equipo separado):
  - Los servicios en src/services/api/ son mocks actualmente
  - Ver PARTE 3 para los contratos de API requeridos


================================================================
4. ARQUITECTURA DEL FRONTEND
================================================================

LAYOUT DEL ÃRBOL DE COMPONENTES:

  App.jsx
  â””â”€â”€ TenantProvider (inyecta branding dinÃ¡mico)
      â”œâ”€â”€ ErrorBoundary
      â””â”€â”€ AuthProvider (inyecta usuario autenticado)
          â””â”€â”€ BrowserRouter
              â”œâ”€â”€ /login           â†’ LoginPage (pÃºblica)
              â”œâ”€â”€ /portal          â†’ PublicLayout â†’ PortalPage (pÃºblica)
              â””â”€â”€ ProtectedRoute
                  â””â”€â”€ DashboardLayout (sidebar + navbar)
                      â”œâ”€â”€ /dashboard
                      â”œâ”€â”€ /usuarios
                      â”œâ”€â”€ /proveedores
                      â”œâ”€â”€ /proveedores/nuevo
                      â”œâ”€â”€ /compras
                      â”œâ”€â”€ /compras/nueva
                      â”œâ”€â”€ /compras/trading
                      â”œâ”€â”€ /compras/:id
                      â”œâ”€â”€ /adjudicacion
                      â”œâ”€â”€ /aprobacion
                      â””â”€â”€ /auditoria

CODE SPLITTING:
  Cada pÃ¡gina se carga con React.lazy() + Suspense. El chunk de la
  pÃ¡gina se descarga solo cuando el usuario navega a esa ruta. El
  loader (spinner giratorio con color del tenant) aparece durante
  la carga. Esto garantiza que el Trading Room no compita por memoria
  con otros mÃ³dulos.

FUNCIÃ“N lazyPage() en App.jsx:
  Las pÃ¡ginas usan named exports ("export function MiPÃ¡gina").
  Como lazy() requiere default export, lazyPage() hace el mapeo:
    const MiPÃ¡gina = lazyPage(() => import('./pages/MiPagina'), 'MiPagina')


================================================================
5. SISTEMA MULTI-TENANT
================================================================

RESOLUCIÃ“N DEL TENANT (resolveTenantId en config/tenants.js):

  En producciÃ³n:
    - URL: sanmiguel.sicst.app â†’ subdominio = "sanmiguel" â†’ tenantId
    - El subdominio es la fuente de verdad absoluta

  En desarrollo local:
    - ?tenant=sanmiguel en la URL   (primera prioridad en dev)
    - localStorage 'sicst_tenant_dev'  (segunda prioridad)
    - DEFAULT_TENANT_ID = 'sanmiguel'  (fallback final)

CICLO DE VIDA DEL TENANTCONTEXT:

  1. App monta â†’ TenantProvider ejecuta su useEffect
  2. resolveTenantId() lee el subdominio
  3. cargarBranding(id) hace GET /api/tenants/:tenantId/branding
  4. Mientras espera â†’ renderiza TenantLoader (spinner #030712)
  5a. Si 404 â†’ renderiza TenantNotFound (pantalla fatal, ninguna pÃ¡gina carga)
  5b. Si ok â†’ inyecta variables CSS en document.documentElement
  6. Renderiza el Ã¡rbol de la aplicaciÃ³n con el branding activo

INYECCIÃ“N DE VARIABLES CSS (applyTheme):
  Se escribe en document.documentElement.style cada variable del tema:
    --color-primary, --color-secondary, --color-accent,
    --color-success, --color-warning, --color-danger,
    --color-bg, --color-surface, --color-border,
    --color-text, --color-muted

  Tailwind los consume asÃ­ en tailwind.config.js:
    'tenant-primary': 'rgb(var(--color-primary) / <alpha-value>)'
  Esto permite clases como bg-tenant-primary/50 con opacidad dinÃ¡mica.

AISLAMIENTO DE DATOS:
  En TODOS los servicios, tenantId es el PRIMER filtro aplicado.
  Ninguna consulta devuelve datos de otro tenant, aunque el endpoint
  reciba un tenantId incorrecto. Esto aplica tanto a mock services
  como (obligatoriamente) al backend real.

FALLBACK DE DESARROLLO:
  Si la API no responde (sin backend levantado), TenantContext detecta
  import.meta.env.DEV y carga el config local de tenants.js.
  En producciÃ³n no hay fallback â€” si la API falla, pantalla fatal.


================================================================
6. AUTENTICACIÃ“N Y ROLES DE USUARIO
================================================================

AUTENTICACIÃ“N ACTUAL (mock):
  LoginPage genera un "fakeJwt" local. Cuando el backend estÃ© listo,
  LoginPage debe cambiar a POST /api/auth/login y guardar el token JWT
  real. El resto del sistema no cambia.

AUTHCONTEXT:
  Expone { user, login, logout }
  user tiene: { id, name, email, role, tenantId }

LOS 8 ROLES DEL SISTEMA:

  admin
    Acceso total. Configura usuarios, mÃ³dulos y parÃ¡metros del tenant.
    Ãšnica cuenta que puede modificar la jerarquÃ­a de aprobaciÃ³n.

  comprador
    Crea expedientes de compra, configura proveedores invitados, inicia
    y monitorea subastas. Registra la recepciÃ³n de bienes.

  proveedor
    Ve la sala de subasta de procesos en los que fue invitado.
    Solo ve precios anonimizados (no sabe quiÃ©n es quiÃ©n).
    Presenta sus lances durante la subasta activa.

  evaluador_documental
    Revisa y aprueba/rechaza la documentaciÃ³n legal de los proveedores
    durante la evaluaciÃ³n previa a la subasta.

  evaluador_tecnico
    EvalÃºa las especificaciones tÃ©cnicas de las ofertas.
    Puede emitir dictÃ¡menes tÃ©cnicos vinculantes.

  auditor
    Acceso de solo lectura a toda la bitÃ¡cora de auditorÃ­a.
    Ve identidades completas (a diferencia del proveedor).
    No puede modificar ningÃºn dato.

  autoridad_aprobadora
    Firma o rechaza etapas del circuito de aprobaciÃ³n pre-adjudicaciÃ³n.
    Recibe notificaciones cuando hay expedientes pendientes de firma.
    Las observaciones son obligatorias en caso de rechazo.

  usuario_consulta
    Solo lectura. Puede ver expedientes y sus estados pero no actÃºa.


================================================================
7. ROUTING Y ACCESO POR ROL
================================================================

La fuente Ãºnica de verdad para los roles permitidos por ruta es:
  src/config/navigation.js  â†’  funciÃ³n getAllowedRoles(path)

Cada <Route> privado en App.jsx usa el componente <Guarded>:
  <Guarded path="/aprobacion"><AprobacionPage /></Guarded>
  â†’ Internamente llama ProtectedRoute con allowedRoles=getAllowedRoles("/aprobacion")

Si el usuario no tiene el rol requerido â†’ redirige a /unauthorized.
Si no tiene sesiÃ³n â†’ redirige a /login.

RUTAS PÃšBLICAS (sin auth, sin roles):
  /login    PÃ¡gina de inicio de sesiÃ³n
  /portal   Portal Ciudadano (PublicLayout, accesible a cualquier ciudadano)


================================================================
8. PATRONES DE INGENIERÃA OBLIGATORIOS
================================================================

Estos patrones se aplican en TODOS los mÃ³dulos del sistema.
Un nuevo ingenieros DEBE seguirlos o react-doctor bajarÃ¡ el score.

1. useReducer PARA ESTADO MÃšLTIPLE
   Cuando hay mÃ¡s de un setState relacionado (cargando + datos + error),
   siempre se usa useReducer con una acciÃ³n que los actualiza juntos.
   Evita renders intermedios inconsistentes.

   Ejemplo:
     const initial = { datos: [], cargando: true, error: null }
     function reducer(state, action) {
       if (action.type === 'CARGADO') return { datos: action.datos, cargando: false, error: null }
       if (action.type === 'ERROR')   return { ...state, cargando: false, error: action.msg }
       return state
     }
     const [state, dispatch] = useReducer(reducer, initial)

2. BANDERA "cancelado" EN useEffect ASYNC
   Todo useEffect que haga operaciones async DEBE tener esta bandera
   para evitar actualizar estado en un componente ya desmontado.

   Ejemplo:
     useEffect(() => {
       let cancelado = false
       fetchDatos(tenantId).then(datos => {
         if (!cancelado) dispatch({ type: 'CARGADO', datos })
       })
       return () => { cancelado = true }
     }, [tenantId])

3. Promise.all PARA CARGAS PARALELAS
   Si hay dos o mÃ¡s datos independientes que cargar al montar, siempre
   usar Promise.all en vez de await secuenciales:
     Promise.all([servicio1(id), servicio2(id)]).then(([a, b]) => ...)

4. MODULE-SCOPE STATICS
   Formatters, funciones puras, arrays y objetos que no dependen de
   props/state se declaran FUERA del componente (a nivel mÃ³dulo).
   Nunca dentro del render.
     const CURRENCY_FORMAT = new Intl.NumberFormat('es-AR', {...})  â† CORRECTO
     function pctAhorro(base, adj) { return ... }                   â† CORRECTO

5. EmptyState isMandatory PARA PARÃMETROS NO CONFIGURADOS
   Si un mÃ³dulo requiere que el municipio haya configurado un recurso
   (plantilla de contrato, jerarquÃ­a de aprobaciones, etc.) y ese
   recurso no existe, el mÃ³dulo NO puede mostrarse.
   Se usa <EmptyState isMandatory={true} /> que bloquea la pantalla
   y muestra un mensaje claro al administrador.

   ParÃ¡metros actuales que bloquean si no estÃ¡n configurados:
     - JerarquÃ­a de aprobaciÃ³n    â†’ ?sinJerarquias en URL (demo)
     - Plantilla de contrato      â†’ ?sinPlantillaContrato (demo)
     - Plantilla de orden         â†’ ?sinPlantillaOrden (demo)
     - Documento de conformidad   â†’ ?sinDocConformidad (demo)
     - Modalidades de compra      â†’ ?sinModalidades (demo)

6. useCallback PARA HANDLERS PASADOS COMO PROPS
   Todo handler que se pasa como prop a un hijo debe estar envuelto
   en useCallback para evitar re-renders innecesarios del hijo.

7. BOTONES CON type EXPLÃCITO
   Todo <button> tiene type="button" o type="submit". Sin excepciones.
   Un <button> sin type dentro de un <form> dispara el submit.

8. ACCESIBILIDAD BÃSICA
   - aria-label en controles sin texto visible (Ã­conos solos)
   - scope="col" en <th> de tablas
   - Inputs con id + label htmlFor asociado


================================================================
9. ESTRUCTURA DE ARCHIVOS CLAVE
================================================================

src/
â”œâ”€â”€ config/
â”‚   â”œâ”€â”€ tenants.js          Config local de tenants (dev-only fallback)
â”‚   â””â”€â”€ navigation.js       Ãrbol de nav + getAllowedRoles(path)
â”‚
â”œâ”€â”€ contexts/
â”‚   â”œâ”€â”€ TenantContext.jsx   Branding dinÃ¡mico, App Shell, multi-tenant
â”‚   â””â”€â”€ AuthContext.jsx     Usuario, login, logout, roles
â”‚
â”œâ”€â”€ layouts/
â”‚   â”œâ”€â”€ DashboardLayout.jsx Sidebar colapsable + Navbar (rutas privadas)
â”‚   â””â”€â”€ PublicLayout.jsx    Header mÃ­nimo con logo del tenant (portal)
â”‚
â”œâ”€â”€ components/
â”‚   â”œâ”€â”€ auth/
â”‚   â”‚   â””â”€â”€ ProtectedRoute.jsx   Guard de autenticaciÃ³n y roles
â”‚   â””â”€â”€ ui/
â”‚       â”œâ”€â”€ EmptyState.jsx       Pantalla bloqueante de parÃ¡metros faltantes
â”‚       â”œâ”€â”€ ErrorBoundary.jsx    Captura errores React en producciÃ³n
â”‚       â””â”€â”€ BadgeEstado.jsx      Badge visual de estado de expediente
â”‚
â”œâ”€â”€ services/api/
â”‚   â”œâ”€â”€ apiClient.js         Instancia de axios con interceptors
â”‚   â”œâ”€â”€ adjudicacionService.js
â”‚   â”œâ”€â”€ aprobacionService.js
â”‚   â”œâ”€â”€ arcaService.js       VerificaciÃ³n ARCA (mock + contingencia)
â”‚   â”œâ”€â”€ auditoriaService.js  BitÃ¡cora + logEventoExterno()
â”‚   â”œâ”€â”€ catalogosService.js  Rubros, modalidades, etc.
â”‚   â”œâ”€â”€ expedienteService.js Vista maestra + recepciÃ³n
â”‚   â””â”€â”€ portalService.js     Procesos y documentos pÃºblicos
â”‚
â”œâ”€â”€ pages/
â”‚   â”œâ”€â”€ auth/               Login, Unauthorized
â”‚   â”œâ”€â”€ dashboard/          Dashboard principal
â”‚   â”œâ”€â”€ usuarios/           GestiÃ³n de usuarios
â”‚   â”œâ”€â”€ proveedores/        Lista, wizard nuevo proveedor, ARCA
â”‚   â”œâ”€â”€ compras/
â”‚   â”‚   â”œâ”€â”€ expediente/     5 tabs de la vista maestra
â”‚   â”‚   â”œâ”€â”€ ComprasPage
â”‚   â”‚   â”œâ”€â”€ NuevaCompraPage (wizard 5 pasos)
â”‚   â”‚   â”œâ”€â”€ TradingRoomPage
â”‚   â”‚   â””â”€â”€ DetalleCompraPage
â”‚   â”œâ”€â”€ adjudicacion/       AdjudicacionPage, TimelineAprobacion, VisorPlantillas
â”‚   â”œâ”€â”€ aprobacion/         BandejaAprobaciones, DetalleAprobacion, AprobacionPage
â”‚   â”œâ”€â”€ auditoria/          AuditoriaPage, TablaAuditoria
â”‚   â””â”€â”€ portal/             PortalPage, CardProcesoPublico
â”‚
â”œâ”€â”€ utils/
â”‚   â”œâ”€â”€ cn.js               Helper de clsx + tailwind-merge
â”‚   â””â”€â”€ descargarTxt.js     Descarga de archivos .txt con BOM UTF-8
â”‚
â””â”€â”€ App.jsx                 Routing principal, lazy loading, layouts


================================================================
FIN PARTE 1 â€” Ver PARTE 2 para descripciÃ³n funcional de cada mÃ³dulo
================================================================


